---
name: worktree
description: A set of tools that user wants you to use when he mentions worktree. You should use it when developing things that require a new worktree, or is in a worktree
---

Use my !worktree (in $PATH already so just need to execute it), and inside use !workit -- a tool that helps you run the dev env without port conflicts -- to do development.
